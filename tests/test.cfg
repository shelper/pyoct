# this file specifies the configurations of testing environment
rawdata_file = '' # locate the rawdata for testing

# using pipeline to test the package
## load_raw_data -> adjust_dispersion -> filter
