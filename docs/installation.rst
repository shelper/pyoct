.. highlight:: shell

============
Installation
============

At the command line::

    $ easy_install pypeline

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv pypeline
    $ pip install pypeline
